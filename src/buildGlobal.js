const prebuild = (root, settings, globalServices) => {}

const build = (root, settings, globalServices) => {}

const postbuild = (root, settings, globalServices) => {}

module.exports = {
  prebuild,
  postbuild,
  build,
}
