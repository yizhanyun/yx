const prebuild = async (root, settings, globalServices) => {}

const build = async (root, settings, globalServices) => {}

const postbuild = async (root, settings, globalServices) => {}

export {
  prebuild,
  postbuild,
  build,
}
